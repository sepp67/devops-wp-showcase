- name: Harden proxy (edge)
  hosts: proxy
  become: true
  roles:
    - hardening_proxy

- name: Harden wordpress backends (os-level)
  hosts: wordpress_vms
  become: true
  roles:
    - hardening_os
