- name: Phase 2 - Proxy (Caddy)
  hosts: proxy
  become: true
  roles:
    - common
    - docker
    - caddy_proxy

- name: Phase 2 - WordPress backend nodes
  hosts: wordpress_nodes
  become: true
  roles:
    - common
    - docker
    - wordpress_backend
