- name: restart fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted

- name: restart caddy container
  ansible.builtin.command:
    cmd: docker compose -f {{ project_dir }}/compose.yml restart caddy
  changed_when: true
